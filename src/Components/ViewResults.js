
// List - ordered by most recent games
//
// Who won
// Ordered list of positions

// if RESULTS.js.length === 0 then just ask for first entry

// need to link View Results to Adding of results [VIA RESULTS.js]


const ViewResults = () => {
  return (
    <div>
      
    </div>
  )
}

export default ViewResults
